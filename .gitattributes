* text=auto eol=lf
*.bat text eol=crlf
*.cmd text eol=crlf
*.jpg binary
*.png binary
*.gif binary
*.jar binary
*.exe binary

# 發布 payload 一律逐位元組保存，不做任何行尾轉換。
# commit 的內容 = GitHub Pages 送出的內容 = SHA256SUMS.txt 描述的內容。
# 沒有這條的話 install.bat 會以 LF 存進 blob，下載下來的 checksum 對不上，
# 而且 LF 版的 .bat 在 goto :label 上有實際執行風險。
install/** -text
install/*.jar binary
install/*.exe binary
install/br-sidecar binary
